#
# MAIN
#

# dotfiles
DOTF_BASH_PLUGINS_DIR="$(mktemp -d)"
DOTF_BASH_THEMES_DIR="$(mktemp -d)"
DOTF_BASH_GEN_SETTINGS_FILE="$(mktemp -d)/gen_settings.local"
DOTF_BASE_CM=(base1 base2)
# :dotfiles

PROXY_URL=http://localhost:8080
