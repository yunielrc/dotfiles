#!/usr/bin/env bash

set -euEo pipefail

[[ ! -d ~/.bash-git-prompt ]] || rm --recursive --force ~/.bash-git-prompt
git clone https://github.com/magicmonty/bash-git-prompt.git ~/.bash-git-prompt --depth=1
