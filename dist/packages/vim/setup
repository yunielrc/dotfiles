#!/usr/bin/env bash

set -euEo pipefail

apt-i vim 1> /dev/null

# Configure
git clone --depth=1 https://github.com/amix/vimrc.git ~/.vim_runtime || :
sh ~/.vim_runtime/install_awesome_vimrc.sh

ln --symbolic --force --verbose "${PKG_CONTENT}/my_configs.vim" ~/.vim_runtime/my_configs.vim
