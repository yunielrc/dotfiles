#!/usr/bin/env bash
set -euEo pipefail

# Dependencies
dotf-i brew
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

# Install
brew install bat
# sudo ln --symbolic --force --verbose "$(type -P bat)" /usr/local/bin/bat
